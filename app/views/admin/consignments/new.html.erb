<%= render :partial => 'admin/shared/configuration_menu' %>
 
<h1><%= t("new_consignment") %></h1>
 
<%= error_messages_for :consignment %>
 
<% form_for(:consignment, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag t("create")%> or <%= link_to t("cancel"), admin_consignment_path%>
  </p>
<% end %>